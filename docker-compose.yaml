version: "3.8"

services:
  web:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - extractor

  extractor:
    build: ./services/extractor
    platform: linux/amd64
    ports:
      - "3005:3005"
    restart: always